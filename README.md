# Keybindings:

- All keybinds are in keymaps.lua or lsp.lua. Defaults may not be explicitly defined

# Backend:

- When adding languages, it is necessary to add to both lsp.lua and treesitter.lua
- If adding another plugins folder, it must also be added to lazy-nvim.lua
